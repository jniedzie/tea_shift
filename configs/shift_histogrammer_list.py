from shift_paths import processes, skim, base_path, histograms_path

samples = processes
sample_path = ""

input_directory = f"{base_path}/{sample_path}/{skim}/"
output_dir = f"{base_path}/{sample_path}/{skim}/{histograms_path}/"
