from shift_paths import base_path, processes

input_skim = "initial"
output_skim = "skimmed_allSelections"

samples = processes
sample_path = ""

input_directory = f"{base_path}/{sample_path}/{input_skim}/"
output_dir = f"{base_path}/{sample_path}/{output_skim}/"
